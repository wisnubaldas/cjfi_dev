kind: pipeline
type: docker
name: default

steps:
- name: install
  image: php:7.4.2-apache-buster
  commands:
  - /bin/sh install.sh
  - composer --version
  - composer install

- name: test
  image: php:7.4.27-fpm
  commands:
  - vendor/bin/phpunit --configuration phpunit.xml