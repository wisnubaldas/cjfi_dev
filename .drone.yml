kind: pipeline
type: docker
name: default

steps:
- name: install
  image: composer
  commands:
  - composer install

- name: test
  image: php:latest
  commands:
  - vendor/bin/phpunit --configuration phpunit.xml